export const useNotes = () => {
	// const [notes, setNotes] = useState([]);
	// return { notes, dispatch };
	// const dispatch = action => {
	// 	const newState = getNewState(notes, action);
	// 	setNotes(newState);
	// };
};
